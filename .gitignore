# 外部リポジトリ（サブモジュールとして管理するか、個別にclone）

# SSL証明書
tools/ssl/*.pem

# Node
node_modules/
package-lock.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# ビルド成果物
dist/
build/

# ログ
*.log
.overnight-logs/

# 証明書
tools/certs/

# Local Netlify folder
.netlify
# 上流クローン（8th Wall の web/engine リポジトリ）。参照用に手元に置くだけで
# 追跡しない。エンジン実体は lib/ に、CDN 配布物は lib/vendor/ にコピー済み。
# ここを無効化すると git add -A が 1GB 超の埋め込みリポジトリを巻き込む。
vendor/
